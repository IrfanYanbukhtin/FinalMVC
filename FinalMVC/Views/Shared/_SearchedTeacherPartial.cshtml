@using FinalMVC.DAL.Entities;
@model List<Teacher>

@foreach (var item in Model)
{
    <li>
        <a asp-contoller="teacher" asp-action="details" asp-route-id="@item.Id">
            @item.FullName
        </a>
    </li>
}